{
  "name": "test-polymer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "polymer build",
    "postbuild": "cp -r build/es6-unbundled/* build/",
    "serve": "polymer serve"
  },
  "dependencies": {
    "@polymer/polymer": "^3.5.2"
  },
  "devDependencies": {
    "polymer-cli": "^1.9.11"
  }
}
